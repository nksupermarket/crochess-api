import { Color, Square, PieceType } from './types/types';
declare const Piece: (color: Color, type: PieceType) => {
    hasMove: (from: Square, to: Square) => boolean;
    getPawnCaptures: (origin: Square) => string[] | undefined;
    readonly type: PieceType;
    readonly color: Color;
};
export default Piece;
